version: '3.9'
services:
  eureka-server:
    build: '../eureka-server'
    ports: 
      - '8761:8761'
  rest-app:
    build: .
    depends_on:
      - eureka-server
    ports: 
      - '8081:8080'
    environment:
      - EUREKA_URI=http://eureka-server:8761/eureka
      - SERVER_PORT=8080
  rest-app-b:
    build: '../docker-microservice-b'
    depends_on:
      - eureka-server
    ports: 
      - '8082:8080'
    environment:
      - EUREKA_URI=http://eureka-server:8761/eureka
      - SERVER_PORT=8080
